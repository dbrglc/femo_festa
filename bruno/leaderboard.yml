info:
  name: leaderboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/leaderboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
