info:
  name: order
  type: http
  seq: 4

http:
  method: POST
  url: "{{base-url}}/orders"
  body:
    type: json
    data: |-
      {
        "teamName": "a",
        "score": 12
      }
  auth:
    type: bearer
    token: ciao

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
