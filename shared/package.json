{
  "name": "@repo/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist"
  ],
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}